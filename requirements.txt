# Runtime dependencies installed on Databricks cluster via %pip.
# Only huggingface_hub is needed: the Bronze notebook downloads all.parquet directly
# and reads it with Spark, avoiding the `datasets` library (whose fsspec globbing is
# incompatible with the Databricks runtime's fsspec).
huggingface_hub>=0.24,<2.0
